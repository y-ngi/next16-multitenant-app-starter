# 📖 Drizzle ORM 開発運用マニュアル (How-To)

## 1. 開発の基本フロー（2つのアプローチ）

Drizzle ORM では、開発フェーズに合わせて 2 つの手法を使い分けます。

```text
【日常の開発・試行錯誤】
  schema.ts を編集 ➔ pnpm db:push（マイグレーションファイルを作らず即反映）

【機能完成・本番リリース準備】
  schema.ts を編集 ➔ pnpm db:generate（SQL生成）➔ pnpm db:migrate（適用）

```

---

## 2. シーン別：開発手順

### シーン A：機能開発中・試行錯誤しているとき（`db:push`）

テーブル定義をコロコロ変更する開発初期や、個人のローカル環境で手軽にDB構造を試したい時の手法です。

1. `src/db/schema.ts` にテーブルやカラムを追加・変更する。
2. 以下のコマンドを実行して、ローカル DB に直接構造を同期する。
```bash
pnpm db:push

```


3. `pnpm db:studio` または VS Code（SQLTools）でテーブルが正しく更新されたか確認する。

> **検証方法**: ターミナルに `[✓] Changes applied` と表示されれば適用成功です。

---

### シーン B：機能が完成し、チーム共有・本番反映するとき（`generate` / `migrate`）

Git で DB の変更履歴（マイグレーションファイル）を管理し、他メンバーの環境や本番環境へ安全に反映させる手法です。

1. `src/db/schema.ts` の変更内容を確定させる。
2. **マイグレーションファイルを生成する**（`drizzle/` フォルダに SQL が自動生成されます）：
```bash
pnpm db:generate

```


3. **ローカル DB にマイグレーションを適用する**：
```bash
pnpm db:migrate

```


4. 生成された `drizzle/` 配下の `.sql` ファイルを Git にコミットする。

> **検証方法**: `drizzle/` 内に新しい `.sql` ファイルが生成され、`pnpm db:migrate` 実行後にエラーが出ないことを確認します。

---

### シーン C：他のメンバーが作成した DB 変更を取り込むとき

`git pull` で他のメンバーが作成した新しいマイグレーションファイルを受け取った場合の手順です。

1. 最新のコードを pull する：
```bash
git pull origin main

```


2. 届いたマイグレーションを自身のローカル DB に適用する：
```bash
pnpm db:migrate

```



---

### シーン D：DB の状態がおかしくなった・最初からやり直したいとき

データが崩れたり、接続エラーや外部キー制約でハマった時のリセット手順です。

1. 以下のコマンドで DB を全消去し、テーブル作成と初期データを再投入します：
```bash
pnpm db:reset

```



---

## 3. コマンド使い分けクイックリファレンス

| タイミング / 目的 | 実行するコマンド | 主な用途 |
| --- | --- | --- |
| **スキーマ変更を今すぐ試したい** | `pnpm db:push` | ローカル開発・個人での試行錯誤 |
| **変更をGit管理用にSQL化したい** | `pnpm db:generate` | 機能完成時・PR作成前 |
| **生成されたSQLをDBに反映したい** | `pnpm db:migrate` | 他人の変更取り込み時・本番デプロイ時 |
| **データをグラフィカルに見たい** | `pnpm db:studio` | データ確認・データ手入力 |
| **テスト用初期データを入れたい** | `pnpm db:seed` | 開発生成データのセット |
| **DBを綺麗に作り直したい** | `pnpm db:reset` | 環境の完全初期化 |

---

